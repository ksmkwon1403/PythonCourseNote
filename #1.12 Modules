파이썬에서는 다양한 module을 import해서 사용할 수 있음.
기본적으로 제공되는 module은 따로 설치할 필요 없이 선언만 해주면 됨.
https://docs.python.org/3.5/library/index.html

import math

print(math.ceil(1.2)) #값을 올림하는 함수
print(math.fabs(-1.2)) #절댓값을 반환하는 함수

> 2
> 1.2

자주 사용되는 함수는 datetime, math, csv 등이 있음.

import할 때에는 필요한 함수만 가져올 수도 있음. 

from math import ceil, fsum
으로 math함수에서 ceil과 fsum 함수만 가져올 수 있음. 

그리고 import하면서 함수의 이름도 바꿔줄 수 있음.

from math import fsum as sexy_sum

항상 사용할 함수만 import하도록 하자.

해당 py파일과 같은 폴더에 있는 py파일을 import할 수도 있다.

from [함수가 있는 py파일명] import [불러올 함수 명]
함수를 불러올 때 .py를 붙일 필요 없음.